'use strict'

function findX() {
    const x=1;
  // const x=1;// переменная созданная при пмощи const не может быть объявлена повторно
}
function findY() {
     var y=3;
     var y=9;
   // переменная созданная при помощи var может быть объявлена повторно
   let a=1;
  // let a=2;// переменная созданная при пмощи let не может быть объявлена повторно
}

console.log(findX());
console.log(findY());
