'use strict'

function summ(str) {
    let arr=[]
    arr=str.split('')
    for(let i=0;i<arr.length;i++) {
        if(arr.indexOf(arr[i], i+1)== -1 && arr.lastIndexOf(arr[i],i-1)== -1 )return arr[i]
    }
  return'not found'
}

console.log(summ('rhrghhtty4rfgse3'));
